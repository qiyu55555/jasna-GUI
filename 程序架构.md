# Jasna-GUI 程序架构文档

## 1. 系统概述

Jasna-GUI 是一个基于 Python Tkinter 开发的视频处理图形界面程序，用于调用 jasna-cli.exe 进行视频去马赛克处理，并支持二次修复功能。

### 1.1 主要功能
- 视频批量处理
- 支持多种检测模型（rfdetr-v5, rfdetr-v4, lada-yolo-v4, rfdetr-v3, lada-yolo-v2）
- 检测阈值设置（0.00-1.00，默认0.20）
- 二次修复选项（RTX-SR）
- 视频转码功能
- 卡死监测与自动恢复
- 配置文件自动保存与加载

## 2. 组件架构

### 2.1 核心类结构

```
JasnaGUI (主类)
├── Tooltip (工具提示类)
├── 配置管理
│   ├── save_settings()      # 保存配置
│   └── load_settings()      # 加载配置
├── UI组件创建
│   ├── create_widgets()     # 创建主界面
│   ├── create_video_lists() # 创建视频列表区域
│   └── create_log_area()    # 创建日志区域
├── 视频处理
│   ├── start_processing()   # 开始处理
│   ├── process_single_video() # 处理单个视频
│   └── transcode_video()    # 转码视频
├── 卡死监测
│   ├── start_stuck_monitor() # 启动监测
│   └── stuck_monitor_callback() # 监测回调
└── 工具函数
    ├── get_video_basic_info() # 获取视频信息
    └── show_custom_messagebox() # 自定义弹窗
```

### 2.2 UI布局结构

```
主窗口 (1150x750)
├── 设置区域 (Settings Frame)
│   ├── Jasna程序路径
│   ├── 输入文件夹
│   ├── 输出文件夹
│   ├── 切片帧数 (2个输入框)
│   ├── 检测模型
│   ├── 检测阈值 (默认0.20)
│   └── 输出后缀
├── 二次修复区域 (Secondary Fix Frame)
│   ├── 使用软件 (无/RTX-SR)
│   └── RTX-SR详细设置子模块
│       ├── 缩放 (2X/4X)
│       ├── 质量 (低/中/高/超高)
│       ├── 降噪 (无/低/中/高/超高)
│       └── 去模糊 (无/低/中/高/超高)
├── 视频列表区域
│   ├── 未处理视频列表
│   ├── 处理出错视频列表
│   └── 已处理视频列表
├── 控制按钮区域
│   ├── 开始处理按钮
│   ├── 停止处理按钮
│   └── 二次修复显示/隐藏控制
└── 日志区域
```

## 3. 数据流与处理流程

### 3.1 视频处理流程

```
开始处理
    │
    ▼
遍历未处理视频列表
    │
    ├──► 检查帧率是否标准 (23.976/24/25/29.97/30/59.94/60/120)
    │         │
    │         ├──► 是 ► 正常处理流程
    │         │           │
    │         │           ├──► 构建jasna-cli命令
    │         │           │           │
    │         │           │           ├──► 添加基础参数
    │         │           │           ├──► 添加检测模型参数
    │         │           │           └──► 添加二次修复参数（如启用）
    │         │           │
    │         │           ├──► 启动子进程
    │         │           ├──► 启动卡死监测线程
    │         │           ├──► 监控输出
    │         │           └──► 等待完成
    │         │                       │
    │         │                       ├──► 成功 ► 移动到已处理列表
    │         │                       │
    │         │                       └──► 失败/卡死 ► 转码后重试
    │         │
    │         └──► 否 ► 直接标记为出错 ► 转码后重试
    │
    ▼
处理完成
```

### 3.2 二次修复选项处理

#### TVAI选项
- 参数: `--secondary-restoration tvai`
- 附加参数:
  - `--tvai-ffmpeg-path`: ffmpeg路径
  - `--tvai-model`: 模型名称 (iris-2, proteus-2 等)
  - `--tvai-scale`: 缩放比例 (1/2/4)
  - `--tvai-workers`: 线程数
  - `--tvai-args`: 附加参数

#### RTX-SR选项
- 参数: `--secondary-restoration rtx-super-res`
- 附加参数:
  - `--rtx-scale`: 缩放 (2/4)
  - `--rtx-quality`: 质量 (none/low/medium/high/ultra)
  - `--rtx-denoise`: 降噪 (none/low/medium/high/ultra)
  - `--rtx-deblur`: 去模糊 (none/low/medium/high/ultra)

## 4. 配置文件设计

### 4.1 配置文件路径
`jasna_gui_config.json`

### 4.2 配置项说明

```json
{
    "jasna_path": "jasna程序路径",
    "input_folder": "输入文件夹路径",
    "output_folder": "输出文件夹路径",
    "slice_frames_1": "切片帧数1 (默认30)",
    "slice_frames_2": "切片帧数2 (默认30)",
    "encode_params": "编码参数",
    "transcode_params": "转码参数",
    "output_suffix": "输出后缀 (默认-U)",
    "stuck_seconds": "卡死检测秒数 (默认300)",
    "error_folder": "错误视频存放文件夹",
    "success_folder": "成功视频存放文件夹",
    "slice_frames_history": ["切片帧数历史记录"],
    "detection_model_history": ["检测模型历史记录"],
    "detection_model": "检测模型 (默认rfdetr-v3)",
    "secondary_fix": "二次修复选项 (无/TVAI/RTX-SR)",
    "ffmpeg_path": "TVAI的ffmpeg路径",
    "tvai_model": "TVAI模型 (默认iris-2)",
    "tvai_scale": "TVAI缩放 (默认4)",
    "tvai_threads": "TVAI线程数 (默认2)",
    "tvai_params": "TVAI附加参数",
    "rtx_sr_scale": "RTX-SR缩放 (默认4)",
    "rtx_sr_quality": "RTX-SR质量 (默认high)",
    "rtx_sr_denoise": "RTX-SR降噪 (默认low)",
    "rtx_sr_deblur": "RTX-SR去模糊 (默认low)",
    "detection_threshold": "检测阈值 (默认0.20)",
    "secondary_fix_display": "二次修复显示状态 (显示/隐藏)"
}
```

## 5. 关键功能模块

### 5.1 卡死监测模块

```python
# 启动监测
def start_stuck_monitor(self):
    self.stuck_monitor_thread = threading.Thread(
        target=self.stuck_monitor_callback
    )
    self.stuck_monitor_stop = False
    self.stuck_monitor_thread.start()

# 监测回调
def stuck_monitor_callback(self):
    while not self.stuck_monitor_stop:
        time.sleep(1)
        # 检查进度是否更新
        if time.time() - self.last_progress_time > stuck_seconds:
            self.stuck_detected = True
            # 终止进程并处理
```

### 5.2 视频转码模块

```python
def transcode_video(self, input_path, output_path):
    # 使用FFmpeg进行转码
    cmd = f'ffmpeg -i "{input_path}" {transcode_params} "{output_path}"'
    subprocess.run(cmd, shell=True)
```

### 5.3 命令构建模块

```python
# 基础命令
cmd = f'.\jasna-cli.exe --input "{input}" --output "{output}" --max-clip-size {slice_frames} --codec hevc --encoder-settings {encode_params} --log-level info --detection-model {model} --detection-score-threshold {threshold}'

# 添加二次修复参数
if secondary_fix == "RTX-SR":
    cmd += f' --secondary-restoration rtx-super-res --rtx-quality {quality} --rtx-denoise {denoise} --rtx-deblur {deblur} --rtx-scale {scale}'
```

## 6. 接口定义

### 6.1 jasna-cli.exe 命令行接口

| 参数 | 类型 | 说明 |
|------|------|------|
| `--input` | string | 输入视频文件路径 |
| `--output` | string | 输出视频文件路径 |
| `--max-clip-size` | int | 切片帧数 |
| `--codec` | string | 编码器 (hevc/h264) |
| `--encoder-settings` | string | 编码器设置参数 |
| `--log-level` | string | 日志级别 (debug/info/warn/error) |
| `--detection-model` | string | 检测模型名称 |
| `--detection-score-threshold` | float | 检测阈值 (0.00-1.00，默认0.20) |
| `--secondary-restoration` | string | 二次修复类型 (rtx-super-res) |
| `--rtx-scale` | string | RTX-SR缩放 (2/4) |
| `--rtx-quality` | string | RTX-SR质量 (none/low/medium/high/ultra) |
| `--rtx-denoise` | string | RTX-SR降噪 (none/low/medium/high/ultra) |
| `--rtx-deblur` | string | RTX-SR去模糊 (none/low/medium/high/ultra) |

### 6.2 内部函数接口

#### 翻译函数
```python
def translate_rtx_option_to_english(self, chinese_option: str) -> str:
    """
    将RTX-SR中文选项转换为英文
    
    输入:
        chinese_option: 中文选项 (无/低/中/高/超高)
    
    输出:
        英文选项 (none/low/medium/high/ultra)
    """

def translate_rtx_option_to_chinese(self, english_option: str) -> str:
    """
    将RTX-SR英文选项转换为中文
    
    输入:
        english_option: 英文选项 (none/low/medium/high/ultra)
    
    输出:
        中文选项 (无/低/中/高/超高)
    """
```

## 7. 错误处理

### 7.1 常见错误类型

| 错误类型 | 处理方式 |
|---------|---------|
| 视频帧率非标准 | 直接转码后重试 |
| 处理卡死 | 终止进程，转码后重试 |
| 模型编译失败 | 提示用户调低切片帧数 |
| 文件不存在 | 记录错误并跳过 |

### 7.2 日志记录

使用Python logging模块，日志级别包括：
- DEBUG: 调试信息
- INFO: 一般信息
- WARNING: 警告信息
- ERROR: 错误信息

## 8. 依赖库

```
tkinter      # GUI框架
pymediainfo  # 视频信息获取
logging      # 日志记录
json         # 配置文件处理
subprocess   # 子进程管理
threading    # 多线程支持
```

## 9. 版本历史

| 版本 | 日期 | 主要更新 |
|------|------|---------|
| v5.3 | 2026-03-15 | 添加RTX-SR二次修复选项，移除Swin2SR |
| v5.2 | - | 添加Swin2SR二次修复选项 |
| v5.1 | - | 添加TVAI二次修复选项 |
| v5.0 | - | 初始版本 |

## 10. 注意事项

1. 配置文件使用JSON格式，保存时进行原子写入操作
2. RTX-SR选项值在配置文件中保存为英文（none/low/medium/high/ultra）
3. 界面显示为中文（无/低/中/高/超高）
4. 卡死监测线程与主处理线程分离，避免阻塞
5. 视频处理采用串行模式，一个视频处理完成后再处理下一个
